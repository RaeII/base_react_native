import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#0F172A",
        padding: 16
    },
    title: {
        color: "#f8fafc",
        fontSize: 24,
        fontWeight: "bold",
        marginBottom: 16
    }
})